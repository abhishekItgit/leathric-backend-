spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/leathric_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: Zxcvbnm@2026
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        jdbc:
          batch_size: 20
  jackson:
    serialization:
      write-dates-as-timestamps: false

server:
  port: 8080

security:
  jwt:
    secret: change-me-to-a-very-long-secret-key-at-least-32-bytes
    expiration-ms: 86400000

logging:
  level:
    org.hibernate.SQL: warn
