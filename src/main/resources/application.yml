spring:
  datasource:
    url: jdbc:mysql://database-1.c7cokmqwit9z.ap-south-1.rds.amazonaws.com:3306/leathric_db?useSSL=false&requireSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&connectTimeout=60000&socketTimeout=60000    #username: ${DB_USERNAME}
   # username: admin
     username:${DB_USERNAME}
    password: ${DB_PASSWORD}
   # password: Zxcvbnm2026Zxcvbnm
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        jdbc:
          batch_size: 20

  jackson:
    serialization:
      write-dates-as-timestamps: false

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080

security:
  jwt:
    secret: change-me-to-a-very-long-secret-key-at-least-32-bytes
    expiration-ms: 86400000

logging:
  level:
    org.hibernate.SQL: warn

app:
  storage:
    max-file-size-bytes: 5242880